apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-admin-secret
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
data:
  password: {{ .Values.adminPassword | b64enc | quote }}
